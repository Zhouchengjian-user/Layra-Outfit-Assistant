# LAYRA 2026-09-06 发布记录

目标：现有 veFaaS 镜像函数 `yida-ai-outfit`（`u641nxe7`），沿用既有网关入口。

本次前端包含当前工作区的新版工作室、衣柜轮播和侧栏间距调整。发布前 `npm run typecheck`、54 项测试、ESLint 和 Next.js 生产构建均通过。

常驻实例配置纠正为 min=1 / max=1；保留原有登录、TOS、模型服务和 SQLite `/tmp/data/yida.sqlite` 配置。发布使用单版本全量切换。

标准 Docker 跨平台依赖安装过慢，本次发布使用已通过检查的 Next standalone 产物打包；排除 `.data`、`.env*`、构建缓存和 Darwin 二进制。Linux musl x64 sharp 0.35.4 / libvips 1.3.3 来自原线上镜像，已核对版本并在最终容器内验证生成 WebP 成功。全套 JavaScript 依赖随本次 standalone 产物复制。底层 Node 镜像固定 digest，运行用户为 node。

镜像：`yida-01-cn-beijing.cr.volces.com/yida/yida-ai-outfit:release-20260906`。
镜像 manifest digest：`sha256:a2353d9f8ab31546d76f5efd1299aaf207af6df4e1054817b46aa1e193ae7ea8`。

容器验证：主页 HTTP 200，未配置认证时保护 API 返回 503（按设计拒绝访问），镜像中不含 `.data` 或 `.env.local`，Linux sharp 输出验证通过。

新版宣传片：45 秒，1920×1080 / 30fps，H.264 yuv420p + AAC，MP4 faststart。成片约 10.7 MB，全片解码通过；网页中验证 45 秒时长、1920×1080 分辨率、正常播放和跳转至 33 秒。

宣传片网页与资源发布于 GitHub Pages，提交 `f29c997`；制作工程在 `video/layra-promo/`，制作与素材说明见其中 `CURRENT_RELEASE.md`。

发布完成：revision 6，发布记录 `ij0fkpcvtv1slhe9`。平台在全量目标发布期间曾报告内部过渡流量 70%，随后自动完成切换；未保留灰度版本。发布后沿用原会话登录成功，抽检账号衣柜的 32 个单品 ID 集合与发布前完全一致，个人模特接口读取成功。
